import{H as e,ht as t,nt as n,tt as r}from"./chunks/framework.qcK96eCy.js";var i=JSON.parse(`{"title":"Asking a person","description":"","frontmatter":{},"headers":[],"relativePath":"guide/approval.md","filePath":"guide/approval.md","lastUpdated":1788340764000}`),a={name:`guide/approval.md`};function o(e,i,a,o,s,c){return t(),r(`div`,null,[...i[0]||=[n(`<h1 id="asking-a-person" tabindex="-1">Asking a person <a class="header-anchor" href="#asking-a-person" aria-label="Permalink to “Asking a person”">​</a></h1><p>Ten of the 22 tools can decide who answers for a name, and DNS has no undo. Those ten <strong>ask a person first</strong>.</p><p>Not a <code>confirm: true</code> argument the model can set. Not a token the model reads out of its own previous result. A dialog, raised through <a href="https://modelcontextprotocol.io/specification/2025-11-25/client/elicitation" target="_blank" rel="noreferrer">MCP elicitation</a>, that goes to the client and is shown to whoever is sitting there.</p><p>The specification says a client <em>should</em> keep a human in the loop:</p><blockquote><p>there <strong>SHOULD</strong> always be a human in the loop with the ability to deny tool invocations</p></blockquote><p>This server does not rely on that. It raises the question itself, and until an answer comes back, nothing happens.</p><h2 id="what-asks-and-when" tabindex="-1">What asks, and when <a class="header-anchor" href="#what-asks-and-when" aria-label="Permalink to “What asks, and when”">​</a></h2><table tabindex="0"><thead><tr><th>Tool</th><th>When it asks</th></tr></thead><tbody><tr><td><code>delete_zone</code></td><td>always</td></tr><tr><td><code>delete_rrset</code></td><td>always</td></tr><tr><td><code>import_zonefile</code></td><td>always, bound to the exact zone file</td></tr><tr><td><code>set_records</code></td><td>always, bound to the exact record list</td></tr><tr><td><code>remove_records</code></td><td>always, bound to the exact record list</td></tr><tr><td><code>change_primary_nameservers</code></td><td>always, bound to the exact server list</td></tr><tr><td><code>change_zone_protection</code></td><td>only when it <strong>removes</strong> protection</td></tr><tr><td><code>change_rrset_protection</code></td><td>only when it <strong>removes</strong> protection</td></tr><tr><td><code>create_rrset</code></td><td>when the record decides who answers</td></tr><tr><td><code>add_records</code></td><td>when the record decides who answers</td></tr><tr><td>everything else</td><td>never</td></tr></tbody></table><p>Protection is the asymmetric case. Switching it <em>on</em> costs nothing and is asked about by nobody; switching it off is the step that makes the next delete possible, and that is where the question belongs.</p><p><code>update_rrset</code> is deliberately not on the list, and its own annotation used to say it should be — the comment claimed it replaced records “exactly like <code>set_records</code>”. It does not: it replaces an RRSet&#39;s <strong>labels</strong>, which are organisational metadata. Nothing goes off the internet, so nothing is asked. It stays marked destructive, because Hetzner keeps no history of labels either.</p><h2 id="why-adding-is-not-automatically-safe" tabindex="-1">Why adding is not automatically safe <a class="header-anchor" href="#why-adding-is-not-automatically-safe" aria-label="Permalink to “Why adding is not automatically safe”">​</a></h2><p>The first eight entries are about loss. That is the right question for a file and only half of it for a zone: the dangerous act in DNS is <strong>making</strong> a claim, not withdrawing one, and none of the following removes anything.</p><ul><li>an <code>MX</code> at preference <code>0</code> beside the real one wins all mail — senders try ascending preference (RFC 5321 §5.1), and the existing record stays exactly where it was</li><li>an <code>NS</code> on a subname creates a zone cut, and the parent starts issuing referrals for everything beneath it</li><li>a <code>CAA</code> changes which certificate authority may issue at all</li><li><code>CNAME</code>, <code>DS</code>, <code>TLSA</code>, <code>SVCB</code> and <code>SRV</code> each redirect or re-key a name</li></ul><p>So <code>create_rrset</code> and <code>add_records</code> ask when the type is one of those, when the name is the apex <code>@</code> — where SPF, DMARC and the zone&#39;s own <code>NS</code> set live — or when it contains <code>*</code>, which answers every name that does not exist yet. Everything else, <code>www/A</code> included, goes through without a dialog.</p><h3 id="the-deliberate-exception" tabindex="-1">The deliberate exception <a class="header-anchor" href="#the-deliberate-exception" aria-label="Permalink to “The deliberate exception”">​</a></h3><p><code>_acme-challenge</code> <code>TXT</code> records are <strong>not</strong> gated, and that is a decision rather than an oversight. Adding one is how DNS-01 renewal works, its entire purpose is to run unattended, and a dialog on every certificate renewal is a cost with no matching benefit — the confirmation cannot tell a real ACME client from a forged token, because from here the two look identical.</p><p>What defends that name is <code>CAA</code>, which is gated now, plus Certificate Transparency monitoring: a certificate issued behind your back shows up in the CT logs within minutes. If you rely on this server for DNS-01, watch them.</p><h2 id="what-the-dialog-contains" tabindex="-1">What the dialog contains <a class="header-anchor" href="#what-the-dialog-contains" aria-label="Permalink to “What the dialog contains”">​</a></h2><p>Zone names, record-set names, counts — and <strong>the values this call is about to write</strong>, on their own lines under &quot;supplied by the caller, not by this server&quot;.</p><p>That distinction is the point, and it used to be drawn one step too broadly. Values that come <em>back from the API</em> stay out: they are written by whoever controls the zone, which for a zone you have just taken over is not necessarily you, and the prompt is read by a model at the exact moment it is deciding. Values that go <em>into</em> the call are the opposite — they are the thing being decided about, and without them the dialog is byte-identical whether the record points where you meant or somewhere else. &quot;Replace the records of <code>www/A</code> — it currently holds 1 record&quot; is true either way.</p><p>A <code>tsig_key</code> is never printed. The schema calls it a secret, and a dialog is not the place for one.</p><div class="language-"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117;" tabindex="0" dir="ltr"><code><span class="line"><span>This will delete RRSet &quot;www/A&quot; of zone &quot;example.com&quot;.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>It currently holds 2 record(s), TTL 300, and deleting is irreversible. Use</span></span>
<span class="line"><span>get_rrset to review the contents.</span></span></code></pre></div><p>The approval is bound to its target, so one obtained for a call cannot be replayed against another. For a <em>set</em> of targets the binding is a fingerprint of the exact list: an approval for <code>[&quot;a&quot;]</code> does not execute <code>[&quot;a&quot;, &quot;b&quot;]</code>.</p><h2 id="clients-that-cannot-show-a-dialog" tabindex="-1">Clients that cannot show a dialog <a class="header-anchor" href="#clients-that-cannot-show-a-dialog" aria-label="Permalink to “Clients that cannot show a dialog”">​</a></h2><p>Not every MCP client implements elicitation, and a stateless gateway may not be able to speak for the one it is currently serving. Rather than refuse to work — which pushes people towards switching the guard off entirely — the tool falls back to a <strong>two-call token</strong>: the first call returns a random string, the second has to quote it back.</p><p>Be clear about what that proves, because this server is:</p><blockquote><p>the token proves the call was made twice with the same arguments, and nothing more.</p></blockquote><p>A model can read the token out of the first result and call again in the same turn without anybody seeing it. It catches a widened target set; it does not catch a model that was talked into the whole thing. The fallback text says so rather than implying somebody approved.</p><h2 id="switching-the-dialog-off" tabindex="-1">Switching the dialog off <a class="header-anchor" href="#switching-the-dialog-off" aria-label="Permalink to “Switching the dialog off”">​</a></h2><div class="language-sh"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">ELICITATION</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72;">=</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">false</span></span></code></pre></div><p>Default is <code>true</code>. <code>false</code> does <strong>not</strong> remove the guard — it takes the fallback path above, which means the token. There is no setting in which a guarded call goes unannounced.</p><p>Use it where a dialog is the wrong shape rather than an unwanted one: a scheduled job, a test harness, a client whose dialog interrupts something else.</p><div class="warning custom-block"><p class="custom-block-title">It is deliberately not prefixed</p><p><code>ELICITATION</code> has no <code>HETZNER_</code> in front of it, so one <code>export ELICITATION=false</code> — or one <code>-e ELICITATION=false</code> in a compose file — reaches <strong>every</strong> MCP server in that environment, not just this one. That is the point of it and also its risk.</p><p>Two things make it visible rather than silent:</p><ul><li><p>a server started with it off prints one line at startup, in the log of every server it actually reached:</p><div class="language-"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117;" tabindex="0" dir="ltr"><code><span class="line"><span>hetzner-dns-mcp: ELICITATION=false — guarded tools fall back to the two-call token</span></span></code></pre></div></li><li><p>the fallback text names the server that did not ask, instead of blaming a client that was working fine. :::</p></li></ul><p>Anything other than <code>true</code> or <code>false</code> — <code>1</code>, <code>off</code>, <code>yes</code> — <strong>stops the server</strong> with exit code 1 and a message naming both valid values. This is the only variable in this family that defaults to <em>on</em>: a typo that fell back to the default would leave the dialog running while the operator believed it was off, and there would be nothing to tell them.</p><h2 id="annotations-are-the-other-half-and-they-are-only-a-hint" tabindex="-1">Annotations are the other half, and they are only a hint <a class="header-anchor" href="#annotations-are-the-other-half-and-they-are-only-a-hint" aria-label="Permalink to “Annotations are the other half, and they are only a hint”">​</a></h2><p>Every tool of this server declares all four MCP tool annotations — <code>readOnlyHint</code>, <code>destructiveHint</code>, <code>idempotentHint</code>, <code>openWorldHint</code> — so a client can tell before it calls what a call would do. See <a href="/reference/tools">Tools</a>.</p><p>They are advice, and the specification says so:</p><blockquote><p>clients <strong>MUST</strong> consider tool annotations to be untrusted unless they come from trusted servers</p></blockquote><p>An annotation is something a client may ignore. The dialog is not: it is enforced here, on the server side, and no answer means no change. The two are different claims — the annotation says what a call <em>does</em>, the dialog decides whether it <em>happens</em> — which is why a tool can be marked destructive without being guarded. <code>update_zone</code> and <code>change_rrset_ttl</code> are exactly that case: they replace a setting with no history, which is destructive, and they are not asked about.</p><h2 id="behind-a-gateway" tabindex="-1">Behind a gateway <a class="header-anchor" href="#behind-a-gateway" aria-label="Permalink to “Behind a gateway”">​</a></h2><p>Both protocol revisions are handled from one code path. On <code>2025-11-25</code> the question is pushed to the client; on <code>2026-07-28</code> there is no server→client channel at all, so the call returns <code>input_required</code>, ends, and the client retries carrying the answer.</p><p>That answer arrives as ordinary request content, which the SDK does not validate — so the state that ties an answer to its question is sealed (HMAC). A reply whose seal does not open, or opens onto a different target, counts as <strong>no answer</strong> and produces a fresh question rather than an error. The likeliest cause is not an attack: it is a gateway that put the server to sleep while the person was reading.</p><p>If you run this behind <a href="https://github.com/ni-c/mcp-hub" target="_blank" rel="noreferrer">mcp-hub</a>, the hub passes elicitation through in both directions; see its <a href="https://ni-c.github.io/mcp-hub/guide/elicitation" target="_blank" rel="noreferrer">elicitation guide</a>.</p></div>`,33)]])}var s=e(a,[[`render`,o]]);export{i as __pageData,s as default};