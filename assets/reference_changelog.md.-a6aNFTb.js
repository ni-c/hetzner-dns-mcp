import{H as e,ht as t,nt as n,tt as r}from"./chunks/framework.qcK96eCy.js";var i=JSON.parse(`{"title":"Changelog","description":"","frontmatter":{},"headers":[],"relativePath":"reference/changelog.md","filePath":"reference/changelog.md","lastUpdated":1787052895000}`),a={name:`reference/changelog.md`};function o(e,i,a,o,s,c){return t(),r(`div`,null,[...i[0]||=[n(`<h1 id="changelog" tabindex="-1">Changelog <a class="header-anchor" href="#changelog" aria-label="Permalink to “Changelog”">​</a></h1><h2 id="_0-5-0-2026-09-03" tabindex="-1">[0.5.0] - 2026-09-03 <a class="header-anchor" href="#_0-5-0-2026-09-03" aria-label="Permalink to “[0.5.0] - 2026-09-03”">​</a></h2><h3 id="added" tabindex="-1">Added <a class="header-anchor" href="#added" aria-label="Permalink to “Added”">​</a></h3><ul><li><p>Every tool declares an <code>outputSchema</code> and answers with <code>structuredContent</code> beside the text block. A client no longer has to parse prose to use a result.</p><p>All twenty-two carry <code>untrusted: true</code> and <code>source: &quot;hetzner-cloud-api&quot;</code> as fields — there is no exception list, because record values, comments, labels and zone files are written by whoever controls the zone and no tool here answers with anything else. The <code>&lt;untrusted-data&gt;</code> fence stays in the text block, where it is the readable presentation of the same marker.</p><p>The API documents are described as open objects with the top-level keys the spec guarantees. A strict shape would turn a field Hetzner adds into a tool that fails outright, since the SDK validates each result against its schema.</p></li><li><p>Tools that need a confirmation now <strong>ask the user</strong>, on clients that can show a prompt. The two-call <code>confirm_token</code> remains for clients that cannot, so nothing that works today stops working — but where a person can be asked, one is, instead of a token that only proves the same call was made twice.</p></li><li><p><code>ELICITATION</code> switches the dialog off — <code>false</code> sends a client that could have been asked down the two-call-token path instead. For a scheduled job or a test harness, where a dialog is the wrong shape rather than an unwanted one.</p><p>It does <strong>not</strong> remove the guard: there is no setting in which a guarded call goes unannounced. Two deliberate rough edges come with it. The variable is <strong>not prefixed</strong>, so one <code>export ELICITATION=false</code> reaches every MCP server in the environment — which is why a server started with it off prints a line saying so, and why the fallback text names the server instead of blaming a client that was working fine. And a value that is neither <code>true</code> nor <code>false</code><strong>stops the server</strong>, where <code>HETZNER_READ_ONLY</code> right beside it deliberately accepts <code>true</code>, <code>1</code> and <code>yes</code>: this is the only variable here that defaults to <em>on</em>, so failing open on a typo would leave the dialog running while the operator believed it was off. It is read after <code>HETZNER_API_TOKEN</code> is wiped from the environment, so that exit cannot leave the token behind.</p></li><li><p>A <code>docs/guide/approval.md</code> page.</p></li></ul><h3 id="changed" tabindex="-1">Changed <a class="header-anchor" href="#changed" aria-label="Permalink to “Changed”">​</a></h3><ul><li><p>The advertised schemas avoid a spelling that is legal JSON Schema and still gets a tool refused, or its constraint silently dropped, by some MCP clients: an open object now writes <code>&quot;additionalProperties&quot;: true</code> rather than the empty schema <code>{}</code> zod emits for it. What the tools accept and return is unchanged; only the way the schema says so is.</p></li><li><p>An over-budget result is an error rather than a document cut off mid-string. That was fine for a text block and is impossible for <code>structuredContent</code>, which has to parse, and the two channels have to carry the same value.</p></li><li><p>The two-call <code>confirm_token</code> prompt is an error result. What was asked for did not happen, which is what <code>isError</code> says. The text is unchanged and still carries the token.</p></li><li><p><strong>The confirmation gate is drawn around authority, not around loss.</strong> It used to be &quot;eight of the 22 tools can take a name off the internet&quot;; that is the right question for a file and half of it for a zone. The dangerous act in DNS is making a claim, not withdrawing one, and none of these removes anything:</p><ul><li>an <code>MX</code> at preference <code>0</code> beside the real one wins all mail, because senders try ascending preference (RFC 5321 §5.1) and the existing record stays</li><li>an <code>NS</code> on a subname creates a zone cut, and the parent starts issuing referrals for everything beneath it</li><li>a <code>TXT</code> at <code>_acme-challenge</code> is a valid DNS-01 response — RFC 8555 §8.4 says the CA verifies &quot;one of&quot; the records, so an <em>added</em> one is enough for a publicly trusted certificate</li><li>a <code>CAA</code> decides which authority may issue at all</li></ul><p><code>create_rrset</code> and <code>add_records</code> therefore ask when the type is <code>NS</code>, <code>DS</code>, <code>MX</code>, <code>CNAME</code>, <code>CAA</code>, <code>TLSA</code>, <code>SVCB</code>, <code>HTTPS</code> or <code>SRV</code>, when the name is the apex <code>@</code>, or when it contains <code>*</code>. <code>www/A</code> still goes through untouched.</p><p><strong><code>_acme-challenge</code> <code>TXT</code> is deliberately exempt.</strong> Its whole purpose is to run unattended, and a dialog on every certificate renewal buys nothing — the confirmation cannot tell a real ACME client from a forged token. <code>CAA</code> (now gated) and Certificate Transparency monitoring are what defend that name, and the guide says so.</p></li><li><p><strong>The dialog shows the values the call is about to write.</strong> No <code>requestApproval</code> in this repo passed <code>details</code>, so the prompt was byte-identical whether the record pointed where you meant or somewhere else: &quot;replace the records of <code>www/A</code> — it currently holds 1 record&quot; is true either way, and it was the only sentence a person saw. <code>change_primary_nameservers</code> named no address at all.</p><p>The rule that kept them out was applied one step too broadly. Values that come <em>back from the API</em> are written by whoever controls the zone and stay out. Values that go <em>into</em> the call come from the model and are the thing being decided about. <code>renderDetails</code> prints them under &quot;supplied by the caller, not by this server&quot;, collapsed and capped. A <code>tsig_key</code> is never printed.</p></li><li><p><code>ttl</code> is capped at 604800 (one week) instead of the protocol maximum of 2147483647. No resolver honours more — BIND caps at a week, Unbound at a day — so the larger values buy nothing except recovery time for whoever set them: a record served from caches long after it was removed at the authority.</p></li><li><p><strong>BREAKING: the confirmation parameter is now <code>confirm_token</code>, not <code>confirmToken</code>.</strong> This server was the only one of seventeen spelling it in camelCase. A call passing <code>confirmToken</code> is rejected as an unknown argument; pass <code>confirm_token</code> instead. Nothing else about the two-call flow changed.</p></li><li><p>Runs on <strong>MCP SDK 2.0</strong>. Existing clients see the same protocol revision they always did; the change is the package layout behind it, and it is what lets the dialog above work on both protocol eras from one code path — including behind a stateless gateway, where the older mechanism silently fell back to the weaker token for every client.</p></li><li><p>The linter is <strong>oxlint</strong> instead of eslint plus typescript-eslint, which lifts the TypeScript ceiling: typescript-eslint pins <code>typescript</code> below 6.1, so this repository was held on TypeScript 6 by its linter rather than by its code.</p></li><li><p>The tool filter, the confirmation store, the host classifier and the documentation-asset generator now come from <strong><code>mcp-tool-allowlist</code></strong>, <strong><code>mcp-approval</code></strong>, <strong><code>mcp-internal-hosts</code></strong> and <strong><code>svg-asset-set</code></strong> rather than from copies kept here — 863 fewer lines, and one place to fix each. None of them has a runtime dependency of its own.</p></li><li><p>The first call of a guarded tool returns a plain result rather than an error result. It is a question, not a failure, and every other server in the fleet already answered it that way.</p></li><li><p>stdio is served through <code>serveStdio</code>, so the connection&#39;s era is negotiated on the opening exchange rather than assumed. A client that pins the <code>2026-07-28</code> era is served it; until now its <code>server/discover</code> probe was answered with &quot;Method not found&quot; and only <code>2025-11-25</code> was on offer. A client that speaks the older era sees no change — it is still pinned to one instance for the life of the connection, exactly as a hand-wired <code>StdioServerTransport</code> served it.</p></li></ul><h3 id="fixed" tabindex="-1">Fixed <a class="header-anchor" href="#fixed" aria-label="Permalink to “Fixed”">​</a></h3><ul><li><p>Secret redaction and the per-value truncation now run over the structured value as well. Both ran as a <code>JSON.stringify</code> replacer, which reached every string in the document for free; a value handed over as <code>structuredContent</code> is not text, so the same pass has to walk the tree. Without it the two channels of one answer would have differed in exactly the fields this server redacts — and the machine-readable one would have been the unredacted half.</p></li><li><p>The annotation comment on <code>update_rrset</code> claimed it &quot;replaces the records of an RRSet, exactly like <code>set_records</code>&quot;. It replaces an RRSet&#39;s <strong>labels</strong> — organisational metadata — and its description said so all along. The comment was on its way to earning the tool a confirmation dialog it does not need: no name goes off the internet. It stays marked destructive, because Hetzner keeps no history of labels either.</p></li><li><p>Confirmation tokens are compared with a <strong>constant-time</strong> comparison. The copy in this repository used <code>!==</code>, which leaks through timing how much of a guess was right. Reaching a token still requires having received it in a previous tool result, so this closes a margin rather than a hole.</p></li><li><p>A <code>confirm_token</code> that does not match is now refused with the reason — invalid, expired, or issued for different arguments — instead of being answered with a fresh prompt. The second is self-healing when a token merely expired and silent when the token was issued for something else, which is the case the binding exists to catch.</p></li><li><p>An entry in <code>HETZNER_ALLOW_TOOLS</code> that is not tool-name-shaped is now <strong>redacted</strong> in the error rather than quoted back. <code>HETZNER_API_TOKEN</code> and <code>HETZNER_ALLOW_TOOLS</code> are adjacent lines in every compose file, and a paste into the wrong one used to print the credential into the client&#39;s log.</p></li></ul><h2 id="_0-4-0-2026-08-27" tabindex="-1">[0.4.0] - 2026-08-27 <a class="header-anchor" href="#_0-4-0-2026-08-27" aria-label="Permalink to “[0.4.0] - 2026-08-27”">​</a></h2><h3 id="added-1" tabindex="-1">Added <a class="header-anchor" href="#added-1" aria-label="Permalink to “Added”">​</a></h3><ul><li><p><code>HETZNER_ALLOW_TOOLS</code> and <code>HETZNER_DENY_TOOLS</code> choose which of the 22 tools are registered. Both take comma-separated tool names or a prefix with a trailing <code>*</code> (<code>list_*</code>), the allow list decides what is in and the deny list is subtracted from it, and <code>HETZNER_ALLOW_TOOLS=essential</code> selects a curated eight — <code>list_zones</code>, <code>get_zone</code>, <code>list_rrsets</code>, <code>get_rrset</code>, <code>create_rrset</code>, <code>set_records</code>, <code>delete_rrset</code>, <code>export_zonefile</code>. A model picks the right tool far more reliably from eight than from twenty-two, and every visible tool costs context on every request. Nothing changes for an installation that sets neither: all 22 are still registered.</p><p>A filtered tool is not registered at all, so it is absent from <code>tools/list</code> and answers <code>tools/call</code> with &quot;tool not found&quot; — the same cut <code>HETZNER_READ_ONLY</code> already makes, not a second, weaker one.</p><p>An entry that matches no tool <strong>stops the server at startup</strong>, naming the entry and listing the real names, rather than being ignored: an ignored typo leaves a tool missing from <code>tools/list</code> with nothing pointing at the cause. The same applies to a malformed pattern such as <code>*_zone</code>. Under <code>HETZNER_READ_ONLY</code>, an exact write-tool name in the allow list is refused with a message naming the read-only setting instead of calling the tool unknown, while a pattern covering write tools is accepted and simply contributes nothing.</p></li></ul><h3 id="changed-1" tabindex="-1">Changed <a class="header-anchor" href="#changed-1" aria-label="Permalink to “Changed”">​</a></h3><ul><li>The README now carries the same eight badges, in the same order, as every other MCP server in this family, all of them reading from npm rather than hard-coded; the opening follows one shape; and the standalone &quot;Full documentation&quot; line is gone, because the docs badge three lines above it points at the same page.</li></ul><h3 id="fixed-1" tabindex="-1">Fixed <a class="header-anchor" href="#fixed-1" aria-label="Permalink to “Fixed”">​</a></h3><ul><li>The container image no longer ships OpenSSL 3.5.7-r0, which carries <strong>CVE-2026-14456</strong> (denial of service via unbounded memory growth). The pinned <code>node:24-alpine</code> digest is already the newest one; Alpine&#39;s fixed 3.5.8-r0 has simply not been rebuilt into it yet, so the runtime stage now upgrades <code>libcrypto3</code> and <code>libssl3</code> by name. Upgrading those two rather than running a blanket <code>apk upgrade</code> keeps the rest of the image exactly as the digest pins it. The step can go once the base image ships the fix.</li></ul><h2 id="_0-3-3-2026-08-26" tabindex="-1">[0.3.3] - 2026-08-26 <a class="header-anchor" href="#_0-3-3-2026-08-26" aria-label="Permalink to “[0.3.3] - 2026-08-26”">​</a></h2><h3 id="changed-2" tabindex="-1">Changed <a class="header-anchor" href="#changed-2" aria-label="Permalink to “Changed”">​</a></h3><ul><li>Whether <code>HETZNER_API_BASE_URL</code> counts as local — which decides both whether plain <code>http</code> is allowed at all and whether a host other than <code>api.hetzner.cloud</code> is accepted — is now decided by the same host classifier the other MCP servers in this family use, in <code>src/hosts.ts</code>, instead of a list of three exact spellings. <code>127.0.0.2</code>, <code>sub.localhost</code>, <code>localhost.</code> and <code>http://[::ffff:127.0.0.1]</code> are just as local as the three that were listed, and the API token stays on the machine in every one of those cases. The token is still refused over plain <code>http</code> to anything that is not.</li></ul><h2 id="_0-3-2-2026-08-24" tabindex="-1">[0.3.2] - 2026-08-24 <a class="header-anchor" href="#_0-3-2-2026-08-24" aria-label="Permalink to “[0.3.2] - 2026-08-24”">​</a></h2><h3 id="fixed-2" tabindex="-1">Fixed <a class="header-anchor" href="#fixed-2" aria-label="Permalink to “Fixed”">​</a></h3><ul><li>The zone apex was unreachable. Eight of the RRSet tools put the name into the URL path — <code>get_rrset</code>, <code>update_rrset</code>, <code>delete_rrset</code>, <code>set_records</code>, <code>add_records</code>, <code>remove_records</code>, <code>change_rrset_ttl</code> and <code>change_rrset_protection</code> — and every one of them ran it through <code>encodeURIComponent</code>, which turns the apex name <code>@</code> into <code>%40</code>. The Hetzner Cloud API does not decode that: <code>GET /zones/example.com/rrsets/%40/A</code> answers 404 <code>not_found</code> while the RRSet exists and <code>list_rrsets</code> returns it. The A, AAAA, MX and TXT records of the domain itself could therefore be listed but neither read nor changed, and <code>add_records</code> failed with a 422 about invalid syntax. RFC 3986 permits <code>@</code> in a path segment, so the name now goes into the path verbatim. Wildcard names were never affected — <code>encodeURIComponent</code> leaves <code>*</code> alone.</li></ul><h3 id="changed-3" tabindex="-1">Changed <a class="header-anchor" href="#changed-3" aria-label="Permalink to “Changed”">​</a></h3><ul><li><code>rrsetPath()</code> validates the segments it is handed instead of escaping them. The character set — <code>[A-Za-z0-9@*._-]</code>, no slash, no percent sign, no bare <code>.</code> or <code>..</code> — was always what kept a request inside the intended endpoint; <code>encodeURIComponent</code> had nothing left to escape on top of it except the one character it broke. That guard used to live only at the tool boundary and is now re-checked where the path is built, so a future call site cannot skip it.</li><li><code>list_rrsets</code> now documents that its <code>name</code> filter accepts <code>@</code> for the zone apex. The filter always worked — it is a query parameter, not a path segment — but nothing said so.</li></ul><h2 id="_0-3-1-2026-08-18" tabindex="-1">[0.3.1] - 2026-08-18 <a class="header-anchor" href="#_0-3-1-2026-08-18" aria-label="Permalink to “[0.3.1] - 2026-08-18”">​</a></h2><h3 id="fixed-3" tabindex="-1">Fixed <a class="header-anchor" href="#fixed-3" aria-label="Permalink to “Fixed”">​</a></h3><ul><li>The architecture diagram no longer depends on the reader&#39;s operating system. It carried a <code>prefers-color-scheme</code> block, which resolves against the OS rather than the theme toggle of GitHub or npm — so dark-mode readers on a light OS got the light artwork on a dark page. The README now uses <code>&lt;picture&gt;</code>, which is resolved against the page, and the <code>&lt;img&gt;</code> that npm falls back to brings its own card instead of a media query.</li><li><code>docs/.vitepress/config.ts</code> pointed <code>og:image</code> at <code>/og.png</code>, which did not exist — the documentation site had no link preview at all. The file is generated now.</li></ul><h3 id="changed-4" tabindex="-1">Changed <a class="header-anchor" href="#changed-4" aria-label="Permalink to “Changed”">​</a></h3><ul><li>The diagram is generated from a single source, <code>docs/assets/architecture.source.svg</code>, by <code>npm run assets</code>. The four rendered copies had already drifted apart; CI now fails if one of them is edited by hand.</li><li><code>docs/public/og.png</code> is generated at exactly 1280x640, GitHub&#39;s recommended size for a social preview, instead of being drawn by hand.</li></ul><h2 id="_0-3-0-2026-08-16" tabindex="-1">[0.3.0] - 2026-08-16 <a class="header-anchor" href="#_0-3-0-2026-08-16" aria-label="Permalink to “[0.3.0] - 2026-08-16”">​</a></h2><h3 id="added-2" tabindex="-1">Added <a class="header-anchor" href="#added-2" aria-label="Permalink to “Added”">​</a></h3><ul><li><code>Dockerfile</code> (multi-stage, non-root, stdio entrypoint) and <code>.dockerignore</code>, so registries that build and introspect the server in a container no longer have to guess a build.</li><li>Multi-arch container images (amd64/arm64) at <code>ghcr.io/ni-c/hetzner-dns-mcp</code>, published from CI with an SBOM and max-mode build provenance and scanned with Trivy on every push and pull request. <code>server.json</code> lists the image as an OCI package, so the MCP registry offers it alongside the npm package.</li><li><code>HETZNER_READ_ONLY=true</code> registers only the seven read-only tools. The write tools are not registered at all rather than rejected when called, so there is no code path from a write request to the API.</li><li>Documentation site at <a href="https://hetzner-dns-mcp.ni-c.de" target="_blank" rel="noreferrer">hetzner-dns-mcp.ni-c.de</a> with a guide, the full tool reference and the security model.</li></ul><h3 id="changed-5" tabindex="-1">Changed <a class="header-anchor" href="#changed-5" aria-label="Permalink to “Changed”">​</a></h3><ul><li>A missing <code>HETZNER_API_TOKEN</code> no longer exits at startup. The server completes the MCP handshake and lists its tools without credentials; the token is required when a tool actually calls the API, which then fails with the same setup instructions as before. Base URL validation still exits, since a bad base URL can leak the token.</li><li><strong>Breaking:</strong> the <code>confirm</code> boolean is gone. Destructive tools now take an optional <code>confirmToken</code> instead — see below. Callers that passed <code>confirm: true</code> will be refused and handed a token to call again with.</li><li><code>engines.node</code> raised to <code>&gt;=22</code>; Node 20 is end-of-life and is no longer in the CI matrix. The container has been on Node 24 all along.</li><li>Published source maps embed their sources, since only <code>dist/</code> is shipped and the maps previously pointed at a <code>src/</code> that is not in the tarball.</li></ul><h3 id="security" tabindex="-1">Security <a class="header-anchor" href="#security" aria-label="Permalink to “Security”">​</a></h3><ul><li><p><strong>Destructive tools require a server-issued confirmation token.</strong> Every irreversible tool refuses its first call and returns a random, single-use token with a five-minute lifetime; a second call must repeat the identical arguments and pass it. For <code>set_records</code>, <code>remove_records</code>, <code>import_zonefile</code> and <code>change_primary_nameservers</code> the token is bound to a SHA-256 fingerprint of the payload, so a confirmation for one record list cannot write a different one.</p><p>The previous <code>confirm</code> boolean was a value the model set itself, while the refusal messages pasted the current RRSet contents back as raw API JSON. Together that was a self-approving loop: an instruction hidden in a TXT record value or a zone-file comment arrived verbatim in the very message asking for confirmation. A token cannot be produced that way, because it only ever exists in a previous result from this server.</p></li><li><p>Removing protection now counts as destructive. <code>change_zone_protection</code> and <code>change_rrset_protection</code> had no guard at all, so unprotecting a zone and deleting it was two uninterrupted calls; disabling protection is now gated exactly like the deletion it enables, while enabling it stays immediate.</p></li><li><p>Confirmation messages no longer quote anything read back from the API. They report record counts and TTLs only.</p></li><li><p>API responses are wrapped in an <code>&lt;untrusted-data&gt;</code> envelope, keys matching <code>tsig_key</code>, <code>token</code>, <code>secret</code>, <code>password</code> or <code>credential</code> are redacted, single values are truncated at 4 000 characters and whole results at 200 000.</p></li><li><p>Upstream error bodies are truncated at 2 000 characters and HTML error pages — a reverse proxy or WAF in front of the API — are dropped entirely instead of being pasted into the model&#39;s context.</p></li><li><p><code>HETZNER_API_TOKEN</code> and <code>HETZNER_API_BASE_URL</code> are deleted from the environment once read, so a later crash report or diagnostic dump cannot expose them. An unparseable base URL is no longer echoed back, since it can contain a <code>user:token@</code> part.</p></li><li><p><code>mcp-publisher</code> is pinned to a release and verified against its SHA-256 before it runs. It was fetched from <code>/releases/latest</code> unverified, in a job holding <code>id-token: write</code>.</p></li><li><p>The runtime image no longer ships npm, npx or corepack; they are never invoked there, but their vendored dependencies kept appearing in scans.</p></li><li><p>CI additionally runs CodeQL and a Trivy scan of the image for both architectures.</p></li></ul><h2 id="_0-2-3-2026-08-13" tabindex="-1">[0.2.3] - 2026-08-13 <a class="header-anchor" href="#_0-2-3-2026-08-13" aria-label="Permalink to “[0.2.3] - 2026-08-13”">​</a></h2><h3 id="changed-6" tabindex="-1">Changed <a class="header-anchor" href="#changed-6" aria-label="Permalink to “Changed”">​</a></h3><ul><li>zod updated to v4 (the MCP SDK supports <code>^3.25 || ^4.0</code>); <code>z.record()</code> now uses the explicit two-argument form.</li><li>Dev dependencies updated: TypeScript 6 (<code>@types/node</code> is listed explicitly in the tsconfig <code>types</code> field, as TS 6 no longer auto-includes <code>@types</code> packages), <code>@eslint/js</code> 10 (matching ESLint 10).</li></ul><h2 id="_0-2-2-2026-08-11" tabindex="-1">[0.2.2] - 2026-08-11 <a class="header-anchor" href="#_0-2-2-2026-08-11" aria-label="Permalink to “[0.2.2] - 2026-08-11”">​</a></h2><h3 id="added-3" tabindex="-1">Added <a class="header-anchor" href="#added-3" aria-label="Permalink to “Added”">​</a></h3><ul><li>Listed in the official <a href="https://registry.modelcontextprotocol.io" target="_blank" rel="noreferrer">MCP Registry</a> as <code>io.github.ni-c/hetzner-dns-mcp</code>; the release workflow publishes registry updates automatically via GitHub OIDC (<code>server.json</code>, <code>mcpName</code> field).</li></ul><h3 id="changed-7" tabindex="-1">Changed <a class="header-anchor" href="#changed-7" aria-label="Permalink to “Changed”">​</a></h3><ul><li>Dev dependencies updated: vitest 4 (+ matching <code>@vitest/coverage-v8</code>), ESLint 10, <code>@types/node</code> 26; GitHub Actions pins bumped to current major versions. Coverage thresholds rebased to vitest 4&#39;s stricter AST-based measurement.</li></ul><h2 id="_0-2-1-2026-08-11" tabindex="-1">[0.2.1] - 2026-08-11 <a class="header-anchor" href="#_0-2-1-2026-08-11" aria-label="Permalink to “[0.2.1] - 2026-08-11”">​</a></h2><h3 id="added-4" tabindex="-1">Added <a class="header-anchor" href="#added-4" aria-label="Permalink to “Added”">​</a></h3><ul><li>Release workflow: pushing a <code>vX.Y.Z</code> tag runs the tests, publishes to npm via Trusted Publishing (OIDC, with provenance) and creates a GitHub release with the notes from this changelog.</li><li>CI: test matrix extended to Node 24, coverage report (thresholds enforced, uploaded as artifact), weekly <code>npm audit</code> job, Dependabot for npm packages and pinned GitHub Actions.</li></ul><h2 id="_0-2-0-2026-08-11" tabindex="-1">[0.2.0] - 2026-08-11 <a class="header-anchor" href="#_0-2-0-2026-08-11" aria-label="Permalink to “[0.2.0] - 2026-08-11”">​</a></h2><h3 id="changed-8" tabindex="-1">Changed <a class="header-anchor" href="#changed-8" aria-label="Permalink to “Changed”">​</a></h3><ul><li>Renamed the package from <code>mcp-hetzner-dns</code> to <code>hetzner-dns-mcp</code>.</li><li><code>set_records</code>, <code>remove_records</code> and <code>change_primary_nameservers</code> now require <code>confirm=true</code> (like the other destructive tools) and report the current state when refusing; <code>change_primary_nameservers</code> is annotated as destructive.</li></ul><h3 id="security-1" tabindex="-1">Security <a class="header-anchor" href="#security-1" aria-label="Permalink to “Security”">​</a></h3><ul><li><code>HETZNER_API_BASE_URL</code> is validated: https only (http allowed for localhost), URLs with embedded credentials are rejected, and a warning is printed for non-default hosts, since the API token is sent there.</li><li><code>zone</code> and RRSet <code>name</code> parameters are restricted to a safe character set and <code>.</code>/<code>..</code> are rejected, preventing URL path traversal out of the intended API endpoints.</li><li>API requests no longer follow redirects and time out after 30 seconds.</li><li>Fatal errors log only the error message instead of the full stack trace.</li><li>CI: workflow token restricted to <code>contents: read</code>, actions pinned to commit SHAs.</li></ul><h2 id="_0-1-0-2026-08-06" tabindex="-1">[0.1.0] - 2026-08-06 <a class="header-anchor" href="#_0-1-0-2026-08-06" aria-label="Permalink to “[0.1.0] - 2026-08-06”">​</a></h2><h3 id="added-5" tabindex="-1">Added <a class="header-anchor" href="#added-5" aria-label="Permalink to “Added”">​</a></h3><ul><li>Initial release targeting the DNS endpoints of the Hetzner Cloud API (<code>api.hetzner.cloud/v1</code>, Bearer token authentication). The legacy DNS API (<code>dns.hetzner.com</code>) is not supported.</li><li>Zone tools: <code>list_zones</code>, <code>get_zone</code>, <code>create_zone</code>, <code>update_zone</code>, <code>delete_zone</code> (guarded by a <code>confirm</code> parameter), <code>export_zonefile</code>, <code>import_zonefile</code> (guarded by a <code>confirm</code> parameter), <code>change_zone_ttl</code>, <code>change_zone_protection</code>, <code>change_primary_nameservers</code>.</li><li>RRSet tools: <code>list_rrsets</code>, <code>get_rrset</code>, <code>create_rrset</code>, <code>update_rrset</code>, <code>delete_rrset</code> (guarded by a <code>confirm</code> parameter), <code>set_records</code>, <code>add_records</code>, <code>remove_records</code>, <code>change_rrset_ttl</code>, <code>change_rrset_protection</code>.</li><li>Action tools: <code>list_zone_actions</code>, <code>get_zone_action</code>.</li><li>Configuration via <code>HETZNER_API_TOKEN</code>, optional <code>HETZNER_API_BASE_URL</code>.</li></ul>`,54)]])}var s=e(a,[[`render`,o]]);export{i as __pageData,s as default};